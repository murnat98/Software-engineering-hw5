#include <stdio.h>

int main()
{
    int num = 0;

    scanf("%d", &num);

    int count = 0; // How many even digits are in the `num` number

    // TODO: find how many even digits has `num` number and store it in variable `count`

    printf("%d\n", count); // print the count

    return 0;
}
